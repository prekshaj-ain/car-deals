// * USERS
export const USERS_COUNT = 10;
export const ADMINS_COUNT = 1;
export const DEALERSHIPS_COUNT = 5;

// * APP
export const CARS_COUNT = 30;
export const SOLD_VEHICLES_COUNT = 5;
export const MAX_CAR_PER_DEALERSHIP = 10;

export const getRandomNumber = (max) => {
  return Math.floor(Math.random() * max);
};
